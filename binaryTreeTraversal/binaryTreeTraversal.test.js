const binaryTreeTraversals = require('./binaryTreeTraversals');

describe(`In-order traversal of binary search tree`, () => {
  test(`In-order traversal should return node values in increasing order`, () => {
    
  });
});

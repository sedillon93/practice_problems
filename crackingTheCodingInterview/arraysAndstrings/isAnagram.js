'use strict';

const isAnagram = (stringOne, stringTwo) => {
  if(stringOne.length !== stringTwo.length){
    return false;
  }
/*
  map each character as a key in an object with number of occurences as value
  compare the two objects; if every value of every key isn't the same they aren't anagrams
*/
};

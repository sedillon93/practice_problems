'use strict';
const SinglyLinkedList = require('../singlyLinkedList');
const removeMiddleNode = require('./removeMiddleNode');

describe(`removeMiddleNode`, () => {
  test(`removeMiddleNode should remove the specified node; nothing is returned but the list should be altered`, () => {
    
    expect().toBe();
  });
});
